<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <queries>
        <package android:name="com.google.android.apps.maps" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
    </queries>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".BizPalmApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Bizpalm">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.dashboard.DashboardActivity" />
        <activity
            android:name=".ui.inventory.InventoryActivity"
            android:exported="true" />
        <activity android:name=".ui.inventory.AddProductActivity" />
        <activity android:name=".ui.inventory.ScanProductActivity" />
        <activity android:name=".ui.inventory.CheckoutActivity" />
        <activity android:name=".ui.transactions.TransactionHistoryActivity" />
        <activity android:name=".ui.transactions.TransactionDetailActivity" />
        <activity android:name=".ui.analytics.SalesAssistantActivity" />
        <activity android:name=".ui.analytics.AnalyticsActivity" />
        <activity android:name=".ui.dashboard.SettingsActivity" />
        <activity android:name=".ui.dashboard.UserManualActivity" />
        <activity android:name=".ui.dashboard.NearbyStoresActivity" />
        <activity android:name=".ui.analytics.ProductDetailActivity" />
        <activity android:name=".ui.analytics.MonthlyPopularityActivity" />
        <activity android:name=".ui.analytics.ReportPickerActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
