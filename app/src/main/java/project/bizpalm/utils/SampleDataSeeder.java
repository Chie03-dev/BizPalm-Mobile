package project.bizpalm.utils;

import project.bizpalm.data.database.AppDatabase;

public class SampleDataSeeder {
    public static void seed(AppDatabase db) {
        // Disabled for now to prevent compilation errors with new Product schema
    }
}
